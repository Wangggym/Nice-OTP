name: Verify Builds

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  verify-builds:
    name: Verify ${{ matrix.build_target }} build
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        include:
          - os: ubuntu-latest
            build_target: android
          - os: macos-latest
            build_target: ios,macos
    steps:
      - name: Debug Info
        run: |
          echo "Event name: ${{ github.event_name }}"
          echo "Workflow name: ${{ github.event.workflow.name }}"
          echo "Run conclusion: ${{ github.event.workflow_run.conclusion }}"
          echo "Run name: ${{ github.event.workflow_run.name }}"
          echo "Run event: ${{ github.event.workflow_run.event }}"
          echo "Run status: ${{ github.event.workflow_run.status }}"
          echo "Run branch: ${{ github.event.workflow_run.head_branch }}"
          echo "Repository: ${{ github.repository }}"
          
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.1'
          channel: 'stable'
          cache: true
      
      - name: Cache pub dependencies
        uses: actions/cache@v4
        with:
          path: ${{ env.PUB_CACHE }}
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: ${{ runner.os }}-pub-
      
      - name: Install dependencies
        run: flutter pub get

      # Android构建验证
      - name: Verify Android build
        if: matrix.os == 'ubuntu-latest'
        run: flutter build apk --debug
        
      # iOS构建验证
      - name: Verify iOS build
        if: matrix.os == 'macos-latest'
        run: flutter build ios --debug --no-codesign
        
      # macOS构建验证
      - name: Verify macOS build
        if: matrix.os == 'macos-latest'
        run: |
          flutter config --enable-macos-desktop
          flutter build macos --debug