{
  "name": "two_factor_authentication",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "dart run tool/check.dart",
    "test": "flutter test --coverage && genhtml coverage/lcov.info -o coverage/html && open coverage/html/index.html",
    "e2e": "flutter test integration_test --coverage -d macos && genhtml coverage/lcov.info -o coverage/html && open coverage/html/index.html"

  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}